spring:
  application:
    name: api
  profiles:
    active:
    - dev
---
spring:
  config:
    activate:
      on-profile:
      - dev
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
